diff --git a/components/browser/session-storage/src/main/java/mozilla/components/browser/session/storage/AutoSave.kt b/components/browser/session-storage/src/main/java/mozilla/components/browser/session/storage/AutoSave.kt
index 15115fc..36af350 100644
--- a/components/browser/session-storage/src/main/java/mozilla/components/browser/session/storage/AutoSave.kt
+++ b/components/browser/session-storage/src/main/java/mozilla/components/browser/session/storage/AutoSave.kt
@@ -15,7 +15,9 @@ import java.util.concurrent.Executors
 import java.util.concurrent.ScheduledExecutorService
 import java.util.concurrent.ScheduledFuture
 import java.util.concurrent.TimeUnit
+import kotlinx.coroutines.CompletableDeferred
 import kotlinx.coroutines.CoroutineScope
+import kotlinx.coroutines.CoroutineStart
 import kotlinx.coroutines.DelicateCoroutinesApi
 import kotlinx.coroutines.Dispatchers
 import kotlinx.coroutines.GlobalScope
@@ -25,6 +27,7 @@ import kotlinx.coroutines.flow.Flow
 import kotlinx.coroutines.flow.distinctUntilChanged
 import kotlinx.coroutines.flow.map
 import kotlinx.coroutines.launch
+import kotlinx.coroutines.withTimeoutOrNull
 import mozilla.components.browser.state.selector.normalTabs
 import mozilla.components.browser.state.selector.selectedTab
 import mozilla.components.browser.state.state.BrowserState
@@ -51,6 +54,8 @@ class AutoSave(
     internal val logger = Logger("SessionStorage/AutoSave")
     internal var saveJob: Job? = null
     private var lastSaveTimestamp: Long = now()
+    private var saveRequestedWhileBusy = false
+    private var urgentSaveSignal: CompletableDeferred<Unit>? = null
 
     /**
      * Saves the state periodically when the app is in the foreground.
@@ -94,8 +99,8 @@ class AutoSave(
     /**
      * Triggers saving the current state to disk.
      *
-     * This method will not schedule a new save job if a job is already in flight. Additionally it will obey the
-     * interval passed to [SessionStorage.autoSave]; job may get delayed.
+     * Coalesces changes while a save is running, then saves the latest state again. A background save also wakes a
+     * job waiting for the minimum interval. Failed writes are retried and logged.
      *
      * @param delaySave Whether to delay the save job to obey the interval passed to [SessionStorage.autoSave].
      */
@@ -104,7 +109,10 @@ class AutoSave(
         val currentJob = saveJob
 
         if (currentJob != null && currentJob.isActive) {
-            logger.debug("Skipping save, other job already in flight")
+            saveRequestedWhileBusy = true
+            if (!delaySave) {
+                urgentSaveSignal?.complete(Unit)
+            }
             return currentJob
         }
 
@@ -112,27 +120,55 @@ class AutoSave(
         val delayMs = lastSaveTimestamp + minimumIntervalMs - now
         lastSaveTimestamp = now
 
+        val urgentSignal = CompletableDeferred<Unit>()
+        urgentSaveSignal = urgentSignal
+
         @OptIn(DelicateCoroutinesApi::class)
-        GlobalScope.launch(Dispatchers.IO) {
-                if (delaySave && delayMs > 0) {
-                    logger.debug("Delaying save (${delayMs}ms)")
-                    delay(delayMs)
-                }
+        val job = GlobalScope.launch(Dispatchers.IO, start = CoroutineStart.LAZY) {
+            if (delaySave && delayMs > 0) {
+                logger.debug("Delaying save (${delayMs}ms)")
+                withTimeoutOrNull(delayMs) { urgentSignal.await() }
+            }
 
+            var failedAttempts = 0
+            while (true) {
                 val start = now()
+                val saved = try {
+                    sessionStorage.save(store.state)
+                } catch (e: Exception) {
+                    logger.error("Failed to save browser state", e)
+                    false
+                }
 
-                try {
-                    val state = store.state
-                    sessionStorage.save(state)
-                } finally {
-                    val took = now() - start
-                    logger.debug("Saved state to disk [${took}ms]")
+                if (saved) {
+                    failedAttempts = 0
+                    logger.debug("Saved state to disk [${now() - start}ms]")
+                } else {
+                    failedAttempts++
+                    logger.error("Browser state was not saved; the previous session snapshot is still in use")
                 }
+
+                val retryAfterFailure = synchronized(this@AutoSave) {
+                    when {
+                        saveRequestedWhileBusy -> {
+                            saveRequestedWhileBusy = false
+                            true
+                        }
+                        !saved && failedAttempts < MAX_SAVE_ATTEMPTS -> true
+                        else -> {
+                            saveJob = null
+                            urgentSaveSignal = null
+                            false
+                        }
+                    }
+                }
+                if (!retryAfterFailure) break
+                if (!saved) delay(RETRY_DELAY_MILLISECONDS)
             }
-            .also {
-                saveJob = it
-                return it
-            }
+        }
+        saveJob = job
+        job.start()
+        return job
     }
 
     @VisibleForTesting(otherwise = VisibleForTesting.PRIVATE) internal fun now() = SystemClock.elapsedRealtime()
@@ -140,6 +176,8 @@ class AutoSave(
     companion object {
         // Minimum interval between saving states.
         const val DEFAULT_INTERVAL_MILLISECONDS = 2000L
+        private const val MAX_SAVE_ATTEMPTS = 3
+        private const val RETRY_DELAY_MILLISECONDS = 2000L
     }
 }
 
@@ -191,7 +229,7 @@ private class StateMonitoring(private val autoSave: AutoSave) {
             .map { state ->
                 Observation(
                     selectedTabId = state.selectedTabId,
-                    tabs = state.normalTabs.size,
+                    tabIds = state.normalTabs.map { it.id },
                     loading = state.selectedTab?.content?.loading,
                 )
             }
@@ -211,8 +249,8 @@ private class StateMonitoring(private val autoSave: AutoSave) {
             if (lastObservation!!.selectedTabId != observation.selectedTabId) {
                 autoSave.logger.info("Save: New tab selected")
                 true
-            } else if (lastObservation!!.tabs != observation.tabs) {
-                autoSave.logger.info("Save: Number of tabs changed")
+            } else if (lastObservation!!.tabIds != observation.tabIds) {
+                autoSave.logger.info("Save: Tabs changed")
                 true
             } else if (lastObservation!!.loading != observation.loading && observation.loading == false) {
                 autoSave.logger.info("Save: Load finished")
@@ -230,7 +268,7 @@ private class StateMonitoring(private val autoSave: AutoSave) {
 
     private data class Observation(
         val selectedTabId: String?,
-        val tabs: Int,
+        val tabIds: List<String>,
         val loading: Boolean?,
     )
 }
diff --git a/components/browser/session-storage/src/main/java/mozilla/components/browser/session/storage/SessionStorage.kt b/components/browser/session-storage/src/main/java/mozilla/components/browser/session/storage/SessionStorage.kt
index d343e0e..c6680dc 100644
--- a/components/browser/session-storage/src/main/java/mozilla/components/browser/session/storage/SessionStorage.kt
+++ b/components/browser/session-storage/src/main/java/mozilla/components/browser/session/storage/SessionStorage.kt
@@ -62,18 +62,19 @@ class SessionStorage(
 
     @VisibleForTesting internal fun isUriReadable(uri: Uri): Boolean = uri.isReadable(context.contentResolver)
 
-    /** Clears the state saved on disk. */
+    /** Clears the state saved on disk. Returns false if the old snapshot remains. */
     @WorkerThread
-    fun clear() {
-        removeSnapshotFromDisk(context, engine)
+    fun clear(): Boolean = removeSnapshotFromDisk(context, engine).also { cleared ->
+        if (!cleared) {
+            logger.error("Failed to clear browser state; the old session snapshot is still on disk")
+        }
     }
 
     /** Saves the given state to disk. */
     @WorkerThread
     override fun save(state: BrowserState): Boolean {
         if (state.normalTabs.isEmpty()) {
-            clear()
-            return true
+            return clear()
         }
 
         // "about:crashparent" is meant for testing purposes only.  If saved/restored then it will
@@ -94,7 +95,11 @@ class SessionStorage(
         return synchronized(sessionFileLock) {
             try {
                 val file = getFileForEngine(context, engine)
-                stateWriter.write(stateToPersist, file)
+                stateWriter.write(stateToPersist, file).also { saved ->
+                    if (!saved) {
+                        logger.error("Failed to save browser state; available storage: ${context.filesDir.usableSpace} bytes")
+                    }
+                }
             } catch (e: OutOfMemoryError) {
                 crashReporting?.submitCaughtException(e)
                 logger.error("Failed to save state to disk due to OutOfMemoryError", e)
@@ -114,9 +119,11 @@ class SessionStorage(
     }
 }
 
-private fun removeSnapshotFromDisk(context: Context, engine: Engine) {
-    synchronized(sessionFileLock) {
-        getFileForEngine(context, engine).delete()
+private fun removeSnapshotFromDisk(context: Context, engine: Engine): Boolean {
+    return synchronized(sessionFileLock) {
+        val file = getFileForEngine(context, engine)
+        file.delete()
+        !file.baseFile.exists()
     }
 }
 
diff --git a/components/browser/session-storage/src/test/java/mozilla/components/browser/session/storage/AutoSaveTest.kt b/components/browser/session-storage/src/test/java/mozilla/components/browser/session/storage/AutoSaveTest.kt
index 69c1148..463419f 100644
--- a/components/browser/session-storage/src/test/java/mozilla/components/browser/session/storage/AutoSaveTest.kt
+++ b/components/browser/session-storage/src/test/java/mozilla/components/browser/session/storage/AutoSaveTest.kt
@@ -8,11 +8,14 @@ import androidx.lifecycle.Lifecycle
 import androidx.lifecycle.LifecycleOwner
 import androidx.lifecycle.LifecycleRegistry
 import androidx.test.ext.junit.runners.AndroidJUnit4
+import java.util.concurrent.CountDownLatch
 import java.util.concurrent.ScheduledExecutorService
 import java.util.concurrent.ScheduledFuture
 import java.util.concurrent.TimeUnit
 import kotlinx.coroutines.CoroutineScope
 import kotlinx.coroutines.Job
+import kotlinx.coroutines.runBlocking
+import kotlinx.coroutines.withTimeout
 import kotlinx.coroutines.test.StandardTestDispatcher
 import kotlinx.coroutines.test.runTest
 import mozilla.components.browser.state.action.ContentAction
@@ -28,11 +31,14 @@ import mozilla.components.support.test.robolectric.testContext
 import org.junit.Assert.assertNotSame
 import org.junit.Assert.assertNull
 import org.junit.Assert.assertSame
+import org.junit.Assert.assertTrue
 import org.junit.Test
 import org.junit.runner.RunWith
+import org.mockito.Mockito.doAnswer
 import org.mockito.Mockito.doReturn
 import org.mockito.Mockito.mock
 import org.mockito.Mockito.never
+import org.mockito.Mockito.times
 import org.mockito.Mockito.verify
 import org.mockito.Mockito.verifyNoMoreInteractions
 import org.mockito.Mockito.`when`
@@ -43,6 +49,10 @@ class AutoSaveTest {
     private val testDispatcher = StandardTestDispatcher()
     private val scope = CoroutineScope(testDispatcher)
 
+    private fun successfulStorage(): SessionStorage = mock<SessionStorage>().also { storage ->
+        doReturn(true).`when`(storage).save(any())
+    }
+
     @Test
     fun `AutoSave - when going to background`() {
         runTest(testDispatcher) {
@@ -51,7 +61,7 @@ class AutoSaveTest {
             val owner = mock(LifecycleOwner::class.java)
             val lifecycle = LifecycleRegistry(owner)
 
-            val sessionStorage: SessionStorage = mock()
+            val sessionStorage = successfulStorage()
 
             val state = BrowserState()
             val store = BrowserStore(state)
@@ -86,7 +96,7 @@ class AutoSaveTest {
             val state = BrowserState()
             val store = BrowserStore(state)
 
-            val sessionStorage: SessionStorage = mock()
+            val sessionStorage = successfulStorage()
 
             val autoSave =
                 AutoSave(
@@ -114,7 +124,7 @@ class AutoSaveTest {
     @Test
     fun `AutoSave - when tab gets removed`() {
         runTest(testDispatcher) {
-            val sessionStorage: SessionStorage = mock()
+            val sessionStorage = successfulStorage()
 
             val store =
                 BrowserStore(
@@ -166,7 +176,7 @@ class AutoSaveTest {
                     )
                 )
 
-            val sessionStorage: SessionStorage = mock()
+            val sessionStorage = successfulStorage()
 
             val autoSave =
                 AutoSave(
@@ -202,7 +212,7 @@ class AutoSaveTest {
                     )
                 )
 
-            val sessionStorage: SessionStorage = mock()
+            val sessionStorage = successfulStorage()
 
             val autoSave =
                 AutoSave(
@@ -241,7 +251,7 @@ class AutoSaveTest {
                     )
                 )
 
-            val sessionStorage: SessionStorage = mock()
+            val sessionStorage = successfulStorage()
 
             val autoSave =
                 AutoSave(
@@ -269,7 +279,7 @@ class AutoSaveTest {
     @Test
     fun `AutoSave - when tab loading state changes`() {
         runTest(testDispatcher) {
-            val sessionStorage: SessionStorage = mock()
+            val sessionStorage = successfulStorage()
 
             val store =
                 BrowserStore(
@@ -361,7 +371,7 @@ class AutoSaveTest {
 
     @Test
     fun `AutoSave - No new job triggered while save in flight`() {
-        val sessionStorage: SessionStorage = mock()
+        val sessionStorage = successfulStorage()
 
         val state = BrowserState()
         val store = BrowserStore(state)
@@ -374,14 +384,15 @@ class AutoSaveTest {
 
         val runningJob: Job = mock()
         doReturn(true).`when`(runningJob).isActive
+        autoSave.saveJob = runningJob
 
         val saveJob = autoSave.triggerSave()
-        assertSame(saveJob, saveJob)
+        assertSame(runningJob, saveJob)
     }
 
     @Test
     fun `AutoSave - New job triggered if current job is done`() {
-        val sessionStorage: SessionStorage = mock()
+        val sessionStorage = successfulStorage()
 
         val state = BrowserState()
         val store = BrowserStore(state)
@@ -394,8 +405,61 @@ class AutoSaveTest {
 
         val completed: Job = mock()
         doReturn(false).`when`(completed).isActive
+        autoSave.saveJob = completed
 
         val saveJob = autoSave.triggerSave()
         assertNotSame(completed, saveJob)
     }
+
+    @Test
+    fun `AutoSave - retries a failed write`() = runBlocking {
+        val storage: SessionStorage = mock()
+        doReturn(false, true).`when`(storage).save(any())
+        val autoSave = AutoSave(BrowserStore(BrowserState()), storage, minimumIntervalMs = 0)
+
+        autoSave.triggerSave(delaySave = false).join()
+
+        verify(storage, times(2)).save(any())
+    }
+
+    @Test
+    fun `AutoSave - background save interrupts a delayed write`() = runBlocking {
+        val storage = successfulStorage()
+        val autoSave =
+            AutoSave(
+                BrowserStore(BrowserState()),
+                storage,
+                minimumIntervalMs = TimeUnit.MINUTES.toMillis(1),
+            )
+
+        val job = autoSave.triggerSave()
+        assertSame(job, autoSave.triggerSave(delaySave = false))
+        withTimeout(5_000) { job.join() }
+
+        verify(storage, times(2)).save(any())
+    }
+
+    @Test
+    fun `AutoSave - writes again if a change arrives during a write`() = runBlocking {
+        val firstWriteStarted = CountDownLatch(1)
+        val finishFirstWrite = CountDownLatch(1)
+        val storage: SessionStorage = mock()
+        doAnswer {
+            firstWriteStarted.countDown()
+            assertTrue(finishFirstWrite.await(5, TimeUnit.SECONDS))
+            true
+        }.`when`(storage).save(any())
+        val autoSave = AutoSave(BrowserStore(BrowserState()), storage, minimumIntervalMs = 0)
+
+        val job = autoSave.triggerSave(delaySave = false)
+        try {
+            assertTrue(firstWriteStarted.await(5, TimeUnit.SECONDS))
+            assertSame(job, autoSave.triggerSave())
+        } finally {
+            finishFirstWrite.countDown()
+        }
+        job.join()
+
+        verify(storage, times(2)).save(any())
+    }
 }
diff --git a/components/browser/session-storage/src/test/java/mozilla/components/browser/session/storage/SessionStorageTest.kt b/components/browser/session-storage/src/test/java/mozilla/components/browser/session/storage/SessionStorageTest.kt
index a9360f6..fb5fe21 100644
--- a/components/browser/session-storage/src/test/java/mozilla/components/browser/session/storage/SessionStorageTest.kt
+++ b/components/browser/session-storage/src/test/java/mozilla/components/browser/session/storage/SessionStorageTest.kt
@@ -164,7 +164,7 @@ class SessionStorageTest {
         val engine = FakeEngine()
 
         val storage = spy(SessionStorage(testContext, engine))
-        storage.save(BrowserState())
+        assertTrue(storage.save(BrowserState()))
 
         verify(storage).clear()
 
@@ -190,7 +190,7 @@ class SessionStorageTest {
         val persisted = storage.save(state)
         assertTrue(persisted)
 
-        storage.clear()
+        assertTrue(storage.clear())
 
         // Read it back. Expect null, indicating empty.
 
